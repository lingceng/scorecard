# 2026-08 近期迭代记录

## 已稳定体验

- 历史分数改为固定列，对不同位数、不同人数和最新一轮撤销位做了对齐。
- 头像统一深色底板，支持 emoji、字母和短中文；点击头像查看聚合后的称号次数。
- 新局、帮助、昵称、动效和音效收进设置，主计分流程只保留炸弹和提交。
- 增加炸弹 2/4 分直接结算、炸弹元数据、撤销兼容和旧本地数据迁移。
- 结算改为原表格上的透明浮层：金币沿曲线汇总，随后总分连续滚动，稀有称号全局盖章后落回记录位。
- 增加前三皇冠、上一轮“胜”章、分级称号颜色和居中的高分冰雪效果。
- 称号降低发放频率，只保留单局 +10、五/十连胜、翻盘、胜场/领先里程碑和炸弹里程碑。
- 音效默认关闭；关键称号使用逐字对应的本地固定播报，移除普通系统朗读。

## 本轮工程整理

- 将两端共用的计分范围、身份预设、数据兼容和稀有称号判定抽到 `shared/game-rules.js`。
- 删除不再引用的早期普通/中型/连胜语音，避免过时资源继续进入部署包。
- 补充双端体验基线与同步脚本；网页版继续承担动效试验，小程序使用原生滚轮、震动和微信分享能力。
- 双端炸弹入口统一为逐玩家“收 2 / 收 4”一键入账，不再经过赢家、倍率两步确认。

## 2026-08-12

- 调整双端称号发放：炸弹每次必有称号，普通胜局从 +6 开始获得称号。
- 单局分数按 +6、+10、+14、+18 分层；+18 以上新增“传奇”等级，以独立配色、边框和强化粒子效果呈现。
- 将牌桌设置从高频操作区移到历史记录后的页面末尾；设置内移除身份列表，点击头像后在个人战报中直接编辑身份标记。

## 2026-08-13

- 小程序称号舞台改为锚定首尾玩家行的几何中心，不再按整个页面居中。
- 小程序结算时间轴对齐网页版：输方数字先滚动、金币汇入、胜方数字再滚动、称号展开盖章，最后回落到称号记录位。
- 新增 3.65 秒本地结算音效，覆盖筹码流转、数字判定、称号亮相和盖章冲击；固定语音继续只播关键称号。
- 补齐后来新增的 4 个称号语音，并新增称号、voiceKey 与本地 MP3 的完整性检查，防止规则扩展后静默漏播。
